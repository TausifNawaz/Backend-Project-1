# Node.js File System (Create, Read, Update)

This project demonstrates how to perform basic file operations using Node.js and the built-in **File System (`fs`)** module.  
It includes examples of creating, reading, and updating files asynchronously.

---

## 🚀 Features
- Create and write data to a file
- Read file contents
- Append or update data in an existing file
- Error handling for all operations

---

## 🧰 Technologies Used
- **Node.js**
- **File System (`fs`) module**

---

## 🧠 Concepts Covered
| Operation | Method | Description |
|------------|---------|-------------|
| Create / Write | `fs.writeFile()` | Creates a new file or overwrites existing content |
| Read | `fs.readFile()` | Reads data from a file |
| Update / Append | `fs.appendFile()` | Adds new data to the end of an existing file |

---

## 💻 How to Run
1. Clone this repository:
   ```bash
   git clone https://github.com/your-username/nodejs-file-system.git
   cd nodejs-file-system
